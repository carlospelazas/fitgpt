import { FC } from "react";
import DashboardPage from "../components/DashboardPage";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <div>
      <DashboardPage />
    </div>
  );
};

export default page;
